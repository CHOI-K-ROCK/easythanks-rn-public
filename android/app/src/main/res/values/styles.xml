<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>

    </style>

    <!--  theme  -->
    <style name="LightTheme" parent="AppTheme">
        <item name="splashBackgroundColor">@color/background_color_light</item>
        <item name="splashScreen">@drawable/splash_light</item>
    </style>

    <style name="DarkTheme" parent="AppTheme">
        <item name="splashBackgroundColor">@color/background_color_dark</item>
        <item name="splashScreen">@drawable/splash_dark</item>
    </style>

</resources>
